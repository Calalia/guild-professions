//this is a template of the config.js that goes in the config folder make the real file with your information in the same format
const config = { BACKEND_URL: "http://localhost:3001" };
export default config;
